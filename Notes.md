For First Time Check the Git Version Is Install or Not / cmd >> git --version 
If It is showing the version then move Fwd.
Open Git -> code(copy the link Present in the section code.) -> Open the VS Studio -> terminal -> git clone <link> -> cd to the folder
Run Three Command on the Terminal.
git status
git add .
git commit -m "Any Comments"
git push origin main
And its Push to the git 